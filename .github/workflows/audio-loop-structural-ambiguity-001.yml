name: audio-loop-structural-ambiguity-001

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'experiments/audio/audio-loop-structural-ambiguity-001/**'
      - '.github/workflows/audio-loop-structural-ambiguity-001.yml'
  pull_request:
    paths:
      - 'experiments/audio/audio-loop-structural-ambiguity-001/**'
      - '.github/workflows/audio-loop-structural-ambiguity-001.yml'

permissions:
  contents: read

jobs:
  structural-ambiguity:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install pinned CPU inference stack
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch==2.10.0 torchaudio==2.10.0 --index-url https://download.pytorch.org/whl/cpu
          python -m pip install beat-this==1.1.0 soundfile==0.13.1
          python -m pip freeze > structural-ambiguity-pip-freeze.txt

      - name: Run structural ambiguity benchmark
        working-directory: experiments/audio/audio-loop-structural-ambiguity-001
        run: python verify.py

      - name: Show compact evidence
        if: always()
        working-directory: experiments/audio/audio-loop-structural-ambiguity-001
        run: |
          test -f out/evidence.json && cat out/evidence.json || true

      - name: Upload structural ambiguity evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audio-loop-structural-ambiguity-001-evidence
          path: |
            structural-ambiguity-pip-freeze.txt
            experiments/audio/audio-loop-structural-ambiguity-001/out/report.json
            experiments/audio/audio-loop-structural-ambiguity-001/out/evidence.json
            experiments/audio/audio-loop-structural-ambiguity-001/out/fixture/manifest.json
          if-no-files-found: warn
          retention-days: 14
