name: fsld-audition-tuning-001

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'experiments/audio/fsld-audition-tuning-001/**'
      - 'experiments/audio/music-fit-core-001/**'
      - 'experiments/audio/fsld-musicfit-benchmark-001/**'
      - '.github/workflows/fsld-audition-tuning-001.yml'
  push:
    branches: [main]
    paths:
      - 'experiments/audio/fsld-audition-tuning-001/**'
      - 'experiments/audio/music-fit-core-001/**'
      - 'experiments/audio/fsld-musicfit-benchmark-001/**'
      - '.github/workflows/fsld-audition-tuning-001.yml'

permissions:
  contents: read

jobs:
  tune:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      OPENBLAS_NUM_THREADS: 1
      OMP_NUM_THREADS: 1
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Music Fit CPU dependencies
        run: python -m pip install -r experiments/audio/music-fit-core-001/requirements.txt
      - name: Tune on disjoint FSLD IDs and evaluate frozen holdout
        working-directory: experiments/audio/fsld-audition-tuning-001
        run: python tune.py
      - name: Upload report only
        uses: actions/upload-artifact@v4
        with:
          name: fsld-audition-tuning-001-evidence
          path: experiments/audio/fsld-audition-tuning-001/out/report.json
          if-no-files-found: error
          retention-days: 14
